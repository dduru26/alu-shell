#!/usr/bin/env bash
# This script displays the content of the current directory in list format, showing only the part after the first dash in file names.

for file in *; do
    # Check if the file contains a dash, and extract the part after the first dash
    echo "${file#*-}"
done